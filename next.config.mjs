/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   middleware: true,
  // },
  // output: "export",
  // images: { unoptimized: true },
};

export default nextConfig;
